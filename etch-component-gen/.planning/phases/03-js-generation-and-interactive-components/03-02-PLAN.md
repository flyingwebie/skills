---
phase: 03-js-generation-and-interactive-components
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - skills/etch-conventions/references/section-patterns.md
  - skills/css-generation/SKILL.md
autonomous: true
requirements: [GEN-05, QUAL-02]

must_haves:
  truths:
    - "Pricing section template includes toggle group HTML with data-period and data-monthly/data-annual attributes for JS to operate on"
    - "Testimonials section template includes carousel navigation HTML (prev/next buttons, dot indicators) for JS to operate on"
    - "All interactive elements (toggle buttons, carousel buttons, dot indicators) have :focus-visible styles in the CSS generation skill"
    - "CSS generation skill documents interactive-component patterns (scroll-snap carousel, toggle active states)"
  artifacts:
    - path: "skills/etch-conventions/references/section-patterns.md"
      provides: "Updated pricing and testimonials templates with interactive HTML elements"
      contains: "data-period"
    - path: "skills/css-generation/SKILL.md"
      provides: "Interactive component CSS patterns added to the algorithm"
      contains: "scroll-snap"
  key_links:
    - from: "skills/etch-conventions/references/section-patterns.md"
      to: "skills/js-generation/SKILL.md"
      via: "HTML templates include data attributes and ARIA that JS patterns reference"
      pattern: "data-period|aria-pressed|role=\"tablist\""
    - from: "skills/css-generation/SKILL.md"
      to: "skills/js-generation/SKILL.md"
      via: "CSS patterns for interactive states match JS-toggled classes and attributes"
      pattern: "focus-visible|scroll-snap|--active"
---

<objective>
Update section templates with interactive HTML elements and add interactive CSS patterns so the generate pipeline produces complete, working interactive components.

Purpose: GEN-05 requires interactive section types (header/nav, pricing, testimonials) to generate with working JS. The HTML templates need data attributes and interactive elements (pricing toggle group, testimonial carousel nav) that the JS patterns from Plan 01 operate on. QUAL-02 requires :focus-visible on all interactive elements and ARIA attribute support in the CSS.
Output: Updated section-patterns.md with interactive HTML, updated css-generation SKILL.md with interactive CSS patterns
</objective>

<execution_context>
@/Users/davidedelgatto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidedelgatto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-js-generation-and-interactive-components/03-RESEARCH.md
@.planning/phases/03-js-generation-and-interactive-components/03-01-SUMMARY.md

<interfaces>
<!-- From section-patterns.md: Pricing template currently has no toggle group or data attributes -->
<!-- pricing__amount has aria-label but no data-monthly/data-annual -->
<!-- pricing__period has aria-hidden but no data-monthly/data-annual -->

<!-- From section-patterns.md: Testimonials template currently has no carousel nav -->
<!-- testimonials__grid is a plain div with article children -->

<!-- From section-patterns.md: Header template already has mobile toggle + mobile-nav with ARIA -->
<!-- No changes needed for header HTML -->

<!-- From css-generation SKILL.md Step 5: existing :focus-visible on CTAs -->
&__cta { &:focus-visible { outline: 2px solid var(--primary); outline-offset: 2px; } }

<!-- From css-generation SKILL.md Step 8: existing prefers-reduced-motion -->
@media (prefers-reduced-motion: reduce) { transition: none; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interactive HTML elements to pricing and testimonials section templates</name>
  <files>skills/etch-conventions/references/section-patterns.md</files>
  <action>
Update skills/etch-conventions/references/section-patterns.md to add interactive HTML elements to the pricing and testimonials templates. Header template already has all needed ARIA/interactive elements -- no changes needed there.

**Pricing section (section 4):**

1. Add a pricing toggle group BEFORE the pricing__grid div:
```html
<div class="pricing__toggle" role="group" aria-label="Billing period">
  <button class="pricing__toggle-btn pricing__toggle-btn--active" data-period="monthly" aria-pressed="true" type="button">Monthly</button>
  <button class="pricing__toggle-btn" data-period="annual" aria-pressed="false" type="button">Annual</button>
</div>
```

2. Add `data-monthly` and `data-annual` attributes to all `pricing__amount` spans:
   - Starter: `data-monthly="$19" data-annual="$15"`
   - Pro: `data-monthly="$49" data-annual="$39"`
   - Enterprise: leave as "Custom" with no data attributes (no toggle behavior)

3. Add `data-monthly` and `data-annual` attributes to all `pricing__period` spans:
   - `data-monthly="/month" data-annual="/year"`
   - Enterprise has no period span, so no change there

4. Update the Notes section to mention: "Toggle group uses `aria-pressed` for accessible toggle state; `data-period` attributes enable JS to switch displayed prices. JavaScript (from js-generation skill) manages the toggle interaction."

**Testimonials section (section 3):**

1. Add carousel navigation HTML AFTER the closing `</div>` of `testimonials__grid` but still inside `testimonials__container`:
```html
<div class="testimonials__carousel-nav" role="group" aria-label="Testimonial navigation">
  <button class="testimonials__prev" aria-label="Previous testimonial" type="button">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" aria-hidden="true">
      <path d="M15 18l-6-6 6-6" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
    </svg>
  </button>
  <div class="testimonials__dots" role="tablist" aria-label="Testimonial slides">
    <button class="testimonials__dot testimonials__dot--active" role="tab" aria-selected="true" aria-label="Testimonial 1" type="button"></button>
    <button class="testimonials__dot" role="tab" aria-selected="false" aria-label="Testimonial 2" type="button"></button>
    <button class="testimonials__dot" role="tab" aria-selected="false" aria-label="Testimonial 3" type="button"></button>
  </div>
  <button class="testimonials__next" aria-label="Next testimonial" type="button">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" aria-hidden="true">
      <path d="M9 18l6-6-6-6" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
    </svg>
  </button>
</div>
```

2. Update the Notes section to mention: "Carousel navigation uses `role=tablist` with `aria-selected` for dot indicators. CSS scroll-snap handles the scrolling; JavaScript (from js-generation skill) syncs dot indicators and handles keyboard navigation."

**Do NOT modify** the header/nav template (section 7) -- it already has all needed ARIA and interactive elements.
**Do NOT modify** hero, features grid, CTA, or footer templates -- they are Level 0 (no interactivity).
  </action>
  <verify>
    <automated>grep -q "data-period" skills/etch-conventions/references/section-patterns.md && grep -q "data-monthly" skills/etch-conventions/references/section-patterns.md && grep -q "testimonials__carousel-nav" skills/etch-conventions/references/section-patterns.md && grep -q "testimonials__dot" skills/etch-conventions/references/section-patterns.md && grep -q "aria-pressed" skills/etch-conventions/references/section-patterns.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Pricing template has toggle group with data-period, data-monthly/data-annual attributes on price elements, and aria-pressed on toggle buttons. Testimonials template has carousel navigation with prev/next buttons, dot indicators with role=tablist and aria-selected. Header template unchanged (already complete).</done>
</task>

<task type="auto">
  <name>Task 2: Add interactive component CSS patterns to css-generation skill</name>
  <files>skills/css-generation/SKILL.md</files>
  <action>
Update skills/css-generation/SKILL.md to add CSS patterns for interactive components. These patterns are generated when the component has JS (Step 7 produces script.js).

**Add a new section after Step 8 (Accessibility and motion styles), titled "## Interactive Component CSS Patterns":**

Document that these patterns are only added when the component has JavaScript interactivity (Level 1+ per js-generation skill classification). They supplement the standard 8-step algorithm.

**Pattern 1 — Toggle button styling (for pricing toggle):**
```css
&__toggle {
  display: flex;
  justify-content: center;
  gap: var(--space-xs);
  margin-block-end: var(--space-xl);
}

&__toggle-btn {
  padding: var(--space-xs) var(--space-m);
  background: transparent;
  color: var(--text-color);
  border: var(--border-size) solid var(--neutral-light);
  border-radius: var(--btn-radius);
  font-size: var(--text-s);
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease, color 0.2s ease, border-color 0.2s ease;

  &--active {
    background: var(--primary);
    color: var(--neutral-ultra-light);
    border-color: var(--primary);
  }

  &:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
  }
}
```

**Pattern 2 — Carousel CSS (for testimonials scroll-snap):**
```css
&__grid {
  display: flex;
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  gap: var(--grid-gap);
  scrollbar-width: none;

  &::-webkit-scrollbar {
    display: none;
  }
}

&__item {
  flex: 0 0 100%;
  scroll-snap-align: start;
}

@container (width >= 600px) {
  &__item {
    flex: 0 0 calc(50% - var(--grid-gap) / 2);
  }
}

@container (width >= 900px) {
  &__item {
    flex: 0 0 calc(33.333% - var(--grid-gap) * 2 / 3);
  }
}
```

**Pattern 3 — Carousel navigation styling:**
```css
&__carousel-nav {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-m);
  margin-block-start: var(--space-l);
}

&__prev,
&__next {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: var(--space-xs);
  background: transparent;
  border: var(--border-size) solid var(--neutral-light);
  border-radius: var(--radius);
  color: var(--text-color);
  cursor: pointer;
  transition: border-color 0.2s ease, color 0.2s ease;

  &:hover {
    border-color: var(--primary);
    color: var(--primary);
  }

  &:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
  }
}

&__dots {
  display: flex;
  gap: var(--space-xs);
}

&__dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  background: var(--neutral-light);
  border: none;
  cursor: pointer;
  padding: 0;
  transition: background 0.2s ease;

  &--active {
    background: var(--primary);
  }

  &:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
  }
}
```

**Pattern 4 — Mobile nav :focus-visible (for header):**
```css
&__mobile-toggle {
  /* ... existing mobile toggle styles ... */

  &:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
  }
}

&__mobile-link {
  &:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
  }
}
```

Also update the **Anti-Patterns section** to add:
- "NEVER forget `:focus-visible` on interactive elements added for JS interactivity (toggle buttons, carousel nav, dot indicators) — same pattern as CTA focus-visible"

Also update **Step 8 text** to note: "Also add `:focus-visible` on any interactive elements that are part of JS-driven interactions (toggle buttons, carousel buttons, dot indicators) — see Interactive Component CSS Patterns section below."
  </action>
  <verify>
    <automated>grep -q "scroll-snap" skills/css-generation/SKILL.md && grep -q "toggle-btn" skills/css-generation/SKILL.md && grep -q "carousel-nav" skills/css-generation/SKILL.md && grep -q "__dot" skills/css-generation/SKILL.md && grep -c "focus-visible" skills/css-generation/SKILL.md | awk '{if ($1 >= 8) print "PASS"; else print "FAIL: need more focus-visible instances"}'</automated>
  </verify>
  <done>CSS generation skill includes interactive component patterns: toggle button styling with active state and focus-visible, carousel scroll-snap layout, carousel navigation styling with focus-visible on all buttons and dots, mobile toggle focus-visible. All interactive elements have :focus-visible outlines.</done>
</task>

</tasks>

<verification>
1. section-patterns.md pricing template has toggle group with data-period, aria-pressed, data-monthly, data-annual
2. section-patterns.md testimonials template has carousel-nav with prev/next buttons, dots with role=tablist
3. section-patterns.md header template is unchanged (already complete)
4. section-patterns.md hero/features/cta/footer templates are unchanged (Level 0)
5. css-generation SKILL.md has Interactive Component CSS Patterns section
6. css-generation SKILL.md has :focus-visible on toggle buttons, carousel buttons, dot indicators, mobile toggle
7. css-generation SKILL.md has scroll-snap pattern for testimonials carousel
8. css-generation SKILL.md has active/pressed state patterns for pricing toggle and carousel dots
</verification>

<success_criteria>
- Pricing and testimonials section templates include all interactive HTML elements and data attributes needed for JS to operate
- CSS generation skill documents all interactive component patterns with :focus-visible on every interactive element
- Header template remains unchanged (was already complete from Phase 2)
- Static section templates (hero, features, cta, footer) remain unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-js-generation-and-interactive-components/03-02-SUMMARY.md`
</output>
