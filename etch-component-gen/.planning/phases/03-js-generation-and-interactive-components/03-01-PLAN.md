---
phase: 03-js-generation-and-interactive-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/js-generation/SKILL.md
  - commands/generate.md
autonomous: true
requirements: [GEN-04]

must_haves:
  truths:
    - "JS generation skill classifies components by interactivity level (0-4) and selects the correct pattern tier"
    - "Generate pipeline Step 7 activates JS generation for interactive component types and skips for static types"
    - "Generated JavaScript uses vanilla ES2020+ with event delegation, DOMContentLoaded init, and ARIA state management"
  artifacts:
    - path: "skills/js-generation/SKILL.md"
      provides: "Complete JS generation skill with full patterns per interactivity level"
      contains: "Level 0"
    - path: "commands/generate.md"
      provides: "Step 7 activated for conditional JS generation"
      contains: "js-generation"
  key_links:
    - from: "commands/generate.md"
      to: "skills/js-generation/SKILL.md"
      via: "Step 7 reads js-generation skill and classifies component"
      pattern: "js-generation/SKILL.md"
    - from: "commands/generate.md"
      to: "skills/persistence/SKILL.md"
      via: "Step 9 sets hasJs and files.js based on JS generation"
      pattern: "hasJs"
---

<objective>
Complete the JS generation skill with full pattern examples for each interactivity level and activate Step 7 in the /generate pipeline so interactive components receive script.js output.

Purpose: GEN-04 requires a JS generation skill that classifies components by interactivity level and produces vanilla ES2020+ code. The skill skeleton exists but lacks complete patterns. The generate pipeline has Step 7 hardcoded to skip -- it must conditionally generate JS based on classification.
Output: Complete js-generation SKILL.md, updated generate.md with active Step 7
</objective>

<execution_context>
@/Users/davidedelgatto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidedelgatto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-js-generation-and-interactive-components/03-RESEARCH.md
@.planning/phases/02-html-and-css-generation/02-01-SUMMARY.md

<interfaces>
<!-- From commands/generate.md: Step 7 currently reads -->
Step 7 — Generate JS (Skip in Phase 2)
1. If `--no-js` flag is present OR the component type does not require interactivity, skip this step
2. For Phase 2: always skip JS generation regardless of component type
3. If the component type would normally benefit from JavaScript, note this in the output

<!-- From skills/js-generation/SKILL.md: existing skeleton patterns -->
Interactivity levels: Level 0 (none), Level 1 (simple), Level 2 (interactive), Level 3 (dynamic), Level 4 (third-party)
Existing patterns: DOMContentLoaded init, Event delegation, State store, Async fetch

<!-- Steps 8-10 must also be updated to handle JS conditionally -->
Step 8: Write files (currently does not write script.js)
Step 9: Update registry (hasJs: false, files.js: null always)
Step 10: Present summary (no JS code block shown)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete js-generation SKILL.md with full patterns and interactivity decision tree</name>
  <files>skills/js-generation/SKILL.md</files>
  <action>
Replace the "Not Yet Implemented" section and flesh out the existing skeleton in skills/js-generation/SKILL.md with complete, production-ready content:

1. **Add Interactivity Decision Tree** (after the existing classification section):
   - Map component types to levels: hero/features/cta/footer -> Level 0 (skip); header -> Level 1; pricing -> Level 1; testimonials -> Level 1; custom -> infer from description keywords
   - For custom types, check description for keywords: "toggle", "menu", "dropdown", "carousel", "slider", "form", "filter", "sort" -> Level 1+; otherwise Level 0
   - Footer year update is NOT Level 1 -- it is a micro-interaction noted in HTML skill, not a script.js file

2. **Replace existing skeleton patterns** with complete, copy-paste-ready code examples:

   **Level 1 — Mobile Nav Toggle (header/nav):**
   Use the exact pattern from 03-RESEARCH.md "Mobile Navigation Toggle" code example. Key elements: DOMContentLoaded init, querySelector caching, setMenuState function toggling aria-expanded + aria-label + aria-hidden + hidden attribute, click handler, Escape keydown handler, outside-click close, focus management (first link on open, toggle button on close).

   **Level 1 — Pricing Period Toggle:**
   Use the exact pattern from 03-RESEARCH.md "Pricing Monthly/Annual Toggle" code example. Key elements: toggle buttons with data-period + aria-pressed, price elements with data-monthly/data-annual attributes, setPeriod function updating classList/aria-pressed/textContent/aria-label, event delegation via closest().

   **Level 1 — Testimonial Carousel:**
   Use the exact pattern from 03-RESEARCH.md "Testimonial Carousel" code example. Key elements: CSS scroll-snap driven scrolling, goToSlide function using scrollIntoView, prev/next buttons, dot indicators with role=tab + aria-selected, ArrowLeft/ArrowRight keyboard navigation.

   **Keep existing Level 2/3/4 patterns** (state store, async fetch) but note they are for custom components -- the 3 interactive section types above cover Level 1.

3. **Add ARIA State Management section** (new section):
   Document the togglePanel pattern: always update trigger (aria-expanded, aria-label) and panel (aria-hidden, hidden) together in one function. Reference the togglePanel code from research.

4. **Add Keyboard Navigation section** (new section):
   Table of required keyboard interactions per component type (from research Pattern 4). Note that `<button>` elements natively fire click on Enter/Space -- only add keydown for Escape (close) and ArrowLeft/ArrowRight (carousel).

5. **Add HTML Requirements for Interactive Patterns section** (new section):
   Document the additional HTML elements needed when JS is generated:
   - Header/nav: no additions needed (template already has mobile toggle + mobile nav with ARIA)
   - Pricing: needs toggle group (`<div class="pricing__toggle" role="group" aria-label="Billing period">` with monthly/annual buttons using data-period + aria-pressed) and data-monthly/data-annual attributes on price elements
   - Testimonials: needs carousel nav (prev/next buttons + dot indicators with role=tablist) appended after the grid

6. **Add CSS Requirements for Interactive Patterns section** (new section):
   - :focus-visible on all toggle buttons, carousel nav buttons, dot indicators (same pattern as CTA focus-visible in css-generation skill)
   - Active/pressed state for pricing toggle (.pricing__toggle-btn--active)
   - Active dot state (.testimonials__dot--active)
   - Carousel scroll-snap CSS on testimonials__grid (flex, overflow-x, scroll-snap-type, scrollbar hidden)
   - Mobile nav: hidden attribute approach (no CSS transitions needed for v1)

7. **Add Anti-Patterns section** listing the 5 anti-patterns from research (no JS for static components, no querySelector in event handlers, no inline onclick, no framework patterns, document cleanup responsibility).

8. **Remove the "Not Yet Implemented" section entirely.**
  </action>
  <verify>
    <automated>grep -c "Not Yet Implemented" skills/js-generation/SKILL.md | grep -q "^0$" && grep -q "Level 0" skills/js-generation/SKILL.md && grep -q "Level 1" skills/js-generation/SKILL.md && grep -q "aria-expanded" skills/js-generation/SKILL.md && grep -q "DOMContentLoaded" skills/js-generation/SKILL.md && grep -q "scroll-snap" skills/js-generation/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>js-generation SKILL.md is complete with: interactivity decision tree mapping types to levels, full Level 1 patterns for header/pricing/testimonials, ARIA state management section, keyboard navigation section, HTML requirements section, CSS requirements section, anti-patterns section, zero "Not Yet Implemented" markers</done>
</task>

<task type="auto">
  <name>Task 2: Activate Step 7 in generate pipeline and update Steps 8-10 for conditional JS</name>
  <files>commands/generate.md</files>
  <action>
Update commands/generate.md to activate JS generation:

1. **Update the command description** (line 1-2 area): Change "Produces `index.html` and `style.css`" to "Produces `index.html`, `style.css`, and optionally `script.js`"

2. **Replace Step 7** entirely. Remove the "Skip in Phase 2" content. New Step 7:

   ```
   ### Step 7 — Generate JS (conditional)

   1. If `--no-js` flag is present, skip this step entirely
   2. Read `@${CLAUDE_PLUGIN_ROOT}/skills/js-generation/SKILL.md`
   3. Classify the component's interactivity level using the decision tree:
      - `hero`, `features`, `cta`, `footer` -> Level 0, skip JS generation
      - `header` -> Level 1 (mobile nav toggle)
      - `pricing` -> Level 1 (monthly/annual toggle — generate only if description mentions toggle, switch, or billing)
      - `testimonials` -> Level 1 (carousel navigation — generate by default)
      - `custom` -> Check description for keywords: toggle, menu, dropdown, carousel, slider, form, filter, sort -> Level 1; otherwise Level 0
   4. If Level 0, skip — no script.js will be generated
   5. If Level 1+, generate script.js following the matching pattern from js-generation SKILL.md
   6. If generating JS, also apply the "HTML Requirements for Interactive Patterns" from js-generation SKILL.md — add the required data attributes and interactive HTML elements to the HTML generated in Step 5
   7. If generating JS, also apply the "CSS Requirements for Interactive Patterns" from js-generation SKILL.md — add :focus-visible, active states, and scroll-snap CSS to the CSS generated in Step 6
   ```

3. **Update Step 8** — Add conditional script.js writing:
   - After writing index.html and style.css, add: "4. If JS was generated in Step 7, write `script.js` with the generated JavaScript"

4. **Update Step 9** — Update registry entry instructions:
   - Change `files` description: "`js` set to `components/{component-name}/script.js` if JS was generated, `null` otherwise"
   - Change `hasJs`: "`true` if JS was generated in Step 7, `false` otherwise"

5. **Update Step 10** — Add JS code block presentation:
   - After the CSS code block, add: "If JS was generated, present it as a third code block"
   - Update "Files written" to conditionally show script.js
   - Remove the Phase 2 note about "available in a future update" — replace with: "If the component type was classified as Level 0 and no JS was generated, note: 'This component type does not require JavaScript interactivity.'"
  </action>
  <verify>
    <automated>grep -q "Skip in Phase 2" commands/generate.md && echo "FAIL: still has Phase 2 skip" || (grep -q "js-generation/SKILL.md" commands/generate.md && grep -q "script.js" commands/generate.md && grep -q "hasJs" commands/generate.md && grep -q "Level 0" commands/generate.md && echo "PASS" || echo "FAIL")</automated>
  </verify>
  <done>generate.md Step 7 conditionally generates JS based on interactivity classification, Step 8 writes script.js when JS is generated, Step 9 sets hasJs/files.js correctly, Step 10 presents JS code block and removes Phase 2 future-update note</done>
</task>

</tasks>

<verification>
1. js-generation SKILL.md contains zero "Not Yet Implemented" markers
2. js-generation SKILL.md contains complete patterns for all 3 interactive section types (header, pricing, testimonials)
3. js-generation SKILL.md contains interactivity decision tree mapping component types to levels
4. commands/generate.md Step 7 references js-generation SKILL.md and classifies by level
5. commands/generate.md Step 8 conditionally writes script.js
6. commands/generate.md Step 9 conditionally sets hasJs and files.js
7. commands/generate.md Step 10 shows JS code block when generated
8. No references to "Phase 2" skip behavior remain in generate.md Step 7
</verification>

<success_criteria>
- JS generation skill is complete with decision tree, full Level 1 patterns for 3 interactive types, ARIA management, keyboard navigation, HTML/CSS requirements, and anti-patterns
- Generate pipeline conditionally produces script.js for interactive components and skips for static ones
- Registry entries correctly reflect JS presence via hasJs and files.js fields
</success_criteria>

<output>
After completion, create `.planning/phases/03-js-generation-and-interactive-components/03-01-SUMMARY.md`
</output>
