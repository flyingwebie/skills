---
phase: 04-component-library
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - commands/library.md
  - SKILL.md
autonomous: true
requirements:
  - LIB-03

must_haves:
  truths:
    - "/library --paste <name> outputs the full HTML content in a fenced html code block"
    - "/library --paste <name> outputs the full CSS content in a fenced css code block"
    - "/library --paste <name> conditionally includes JS content only when hasJs is true"
    - "Paste mode reports an error when the component name is not found in the registry"
    - "Paste mode reports an error when a registered file path does not exist on disk"
    - "Each code block is labeled with the component's file path as a heading"
  artifacts:
    - path: "commands/library.md"
      provides: "Step 5 paste mode with file reading, conditional JS, and error handling"
      contains: "Step 5"
    - path: "SKILL.md"
      provides: "Updated command table reflecting /library as fully implemented"
      contains: "/library"
  key_links:
    - from: "commands/library.md"
      to: "components/{name}/index.html"
      via: "file read using registry files.html path"
      pattern: "files\\.html"
    - from: "commands/library.md"
      to: "components/{name}/style.css"
      via: "file read using registry files.css path"
      pattern: "files\\.css"
    - from: "commands/library.md"
      to: "components/{name}/script.js"
      via: "conditional file read using registry files.js path when hasJs is true"
      pattern: "hasJs.*files\\.js"
---

<objective>
Add paste mode (Step 5) to library.md for outputting full component content as copy-paste-ready code blocks, and update SKILL.md to reflect the completed /library command.

Purpose: Enable users to retrieve the full HTML, CSS, and optional JS of any generated component via `/library --paste <name>` for direct copy-paste into Etch's code panels. Also finalize the plugin index to show /library as fully functional.

Output: Updated commands/library.md with Step 5 (paste mode), updated SKILL.md command table.
</objective>

<execution_context>
@/Users/davidedelgatto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidedelgatto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-component-library/04-01-SUMMARY.md

@commands/library.md
@skills/persistence/SKILL.md

<interfaces>
<!-- From Plan 01 output: library.md will have Steps 1-4 -->
Step 2 parses --paste <name> flag and sets mode to "paste"
Step 2 extracts the component name from the --paste flag value

Registry component entry fields used by paste mode:
- name (string) -- lookup key
- files.html (string) -- path to index.html
- files.css (string) -- path to style.css
- files.js (string|null) -- path to script.js, null if no JS
- hasJs (boolean) -- determines whether to read/display JS

From SKILL.md (plugin index):
- Commands table lists /generate and /library with status
- /library currently shows as Phase 4 deliverable
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add paste mode step to library.md</name>
  <files>commands/library.md</files>
  <action>
Add Step 5 to commands/library.md after Step 4, before the Notes section:

### Step 5 -- Paste Component (--paste mode)

When Step 2 determined the mode is **paste**:

1. Search the `components` array for an entry where `name` matches the provided component name (exact match, case-insensitive)
2. If no matching component is found, display: "Component '{name}' not found in the registry. Run `/library` to see available components." and stop.
3. Read the file at the path stored in `files.html`. If the file does not exist at that path, display: "File missing: {files.html}. The component files may have been moved or deleted. Use `/generate {name}` to regenerate." and stop.
4. Read the file at the path stored in `files.css`. If missing, display the same error pattern as above with the CSS path.
5. If `hasJs` is `true`, read the file at the path stored in `files.js`. If missing, display the same error pattern.
6. Output the content in this exact format:

```
### {files.html}

\`\`\`html
{html file content}
\`\`\`

### {files.css}

\`\`\`css
{css file content}
\`\`\`
```

7. If `hasJs` is `true`, also output:

```
### {files.js}

\`\`\`javascript
{js file content}
\`\`\`
```

8. After the code blocks, display: "Paste each block into the corresponding Etch panel (HTML, CSS, JS)."

Do NOT skip Steps 1-2 for paste mode -- the registry must still be read and arguments parsed before reaching Step 5. Step 5 is only entered when Step 2 sets mode to "paste". Steps 3-4 (filter and display table) are skipped in paste mode.

Update Step 2 to explicitly note: "If mode is paste, skip Steps 3-4 and proceed to Step 5."
  </action>
  <verify>
Read commands/library.md and confirm: (1) Step 5 exists with all 8 sub-steps, (2) component-not-found error message references `/library`, (3) file-missing error suggests `/generate {name}` to regenerate, (4) output format uses ### headings with fenced code blocks for html/css, (5) JS block is conditional on hasJs, (6) Step 2 references skipping to Step 5 for paste mode.
  </verify>
  <done>library.md Step 5 reads component files from registry paths, outputs labeled fenced code blocks (html, css, conditional js), and handles component-not-found and file-missing errors with actionable messages. Step 2 updated to route paste mode to Step 5.</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md command table</name>
  <files>SKILL.md</files>
  <action>
Read SKILL.md and update the commands table entry for `/library`:
- Change any "Phase 4" or "not yet implemented" status language to reflect that the command is now fully functional
- Ensure the description matches the frontmatter: "Search, filter, and paste previously generated Etch components"
- Keep all other entries in SKILL.md unchanged

This is a minor update -- just the status/description of the /library row in the commands table.
  </action>
  <verify>
Read SKILL.md and confirm the /library entry no longer references "Phase 4" or "not yet implemented" and has the correct description.
  </verify>
  <done>SKILL.md command table shows /library as a fully functional command with accurate description, no Phase 4 or placeholder language remaining.</done>
</task>

</tasks>

<verification>
- library.md has Steps 1-5 covering all four operation modes (list, search, filter, paste)
- Paste output uses labeled headings and fenced code blocks with language hints
- JS output conditional on hasJs field
- Error handling covers: component not found, file missing on disk
- SKILL.md reflects /library as complete
- No "Not Yet Implemented" text remains anywhere
</verification>

<success_criteria>
- /library --paste mode fully specified with file reading, conditional JS, and error handling
- Output format matches the pattern established in the existing library.md skeleton
- SKILL.md updated to reflect completed command
- All LIB requirements (LIB-01, LIB-02, LIB-03) covered across Plans 01 and 02
</success_criteria>

<output>
After completion, create `.planning/phases/04-component-library/04-02-SUMMARY.md`
</output>
